{% extends "JackDeckKeeperBundle::layout.html.twig" %}

{% block content %}

    {# <form action="{{ path('frontend_deck_keeper_new') }}" method="post" {{ form_enctype(form) }}> #}
    {{ form_start(form, {
            'action': path('frontend_deck_keeper_new'),
            'attr': {
                'novalidate': 'novalidate'
            }
        }
    ) }}

        {{ form_widget(form) }}

        <div class="actions">
            <button class="btn primary" type="submit">Create</button>
        </div>

    {{ form_end(form) }}
    {# </form> #}
{% endblock content %}
